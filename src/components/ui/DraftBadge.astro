---
// Rendered only in dev mode — draft posts never exist in production builds.
---
<div class="draft-badge" aria-label="This post is a draft">
	DRAFT
</div>

<style>
	.draft-badge {
		display: inline-block;
		font-family: 'JetBrains Mono', ui-monospace, monospace;
		font-size: 0.6875rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: #92400e;
		background: rgba(146, 64, 14, 0.08);
		border: 1px solid rgba(146, 64, 14, 0.25);
		border-radius: 3px;
		padding: 0.2em 0.6em;
	}
</style>
